import { BodyInterceptorInterceptor } from './body.interceptor.interceptor';

describe('BodyInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new BodyInterceptorInterceptor()).toBeDefined();
  });
});
